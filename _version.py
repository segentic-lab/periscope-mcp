"""Single source of truth for the Periscope version.

Bump this on every release and tag the commit `v<version>` (see CHANGELOG.md).
It is reported to MCP clients via the server's initialize handshake.
"""
__version__ = "0.10.4"
